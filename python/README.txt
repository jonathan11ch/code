#Make sure to use the appropiate virtualenvironment for each application
virtual envs are located at the home directiory: ~/.

#current environments
[tensorflow]
source ~/tensorflow_env/bin/activate

[jupyter]
source ~/virtual_envs/jupyter_env/bin/activate

